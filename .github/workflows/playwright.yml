name: Nightly Playwright Tests
on:
  workflow_call:
    inputs:
      branch:
        required: true
        type: string
        description: The branch to run the tests on
    secrets: 
       SLACK_WEBHOOK:
         required: true
       SUPABASE_URL:
         required: true
       SUPABASE_ANON_KEY:
         required: true

jobs:
  e2e-nightly-test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    environment: develop # TODO - this should come out of this config and env specific configs should provide it as input
    env: 
        SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
        SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
        E2E_TEST_BASE_URL: ${{ vars.E2E_TEST_BASE_URL }}
        NODE_ENV: ${{ vars.NODE_ENV }}
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: lts/*
    - name: Install dependencies
      run: npm install -g pnpm && pnpm install
    - name: Install Playwright Browsers
      working-directory: ./frontend
      run: pnpm exec playwright install --with-deps
    - name: Print environment variables (for debugging)
      run: |
        echo "E2E_TEST_BASE_URL is $E2E_TEST_BASE_URL"
        echo "SUPABASE_URL is $SUPABASE_URL"
    - name: Start Next.js app
      run: |
        pnpm run dev & # Start Next.js app in the background
        NEXTJS_PID=$!  # Capture the PID of the Next.js process
        echo "Started Next.js with PID $NEXTJS_PID"
        echo "NEXTJS_PID=$NEXTJS_PID" >> $GITHUB_ENV  # Store the PID for later use
        echo "Waiting for Next.js app to start..."
        until curl -s --head --request GET http://localhost:3000 | grep "200 OK"; do
          echo "Waiting for Next.js to be available..."
          sleep 5
        done
        echo "Next.js app is up and running!"
    - name: Run Playwright tests
      working-directory: ./frontend
      run: pnpm exec playwright test e2e
    - name: Clean up
      run: |
        if [ -n "$NEXTJS_PID" ]; then
          kill $NEXTJS_PID
          echo "Killed Next.js process with PID $NEXTJS_PID"
        else
          echo "No Next.js process found."
        fi
    - name: Convert JUnit output to CTRF
      working-directory: ./frontend
      run: pnpm exec junit-to-ctrf playwright-report/results.xml -o playwright-report/results.json
    - name: Publish Test Report
      uses: ctrf-io/github-test-reporter@v1
      with:
        report-path: 'frontend/playwright-report/*.json'
      if: always()
  slack-notifier:
    runs-on: ubuntu-latest
    if: ${{ !cancelled() }}
    permissions:
      actions: read
      contents: read
    needs: e2e-nightly-test
    steps:
      - name: Get failure info
        id: failure-info
        run: |
          delimiter=$(openssl rand -hex 8)
          {
            echo "ERRORS_OUTPUT<<$delimiter"
            curl -v -L \
              -H "Accept: application/vnd.github+json" \
              -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
              -H "X-GitHub-Api-Version: 2022-11-28" \
              https://api.github.com/repos/${{ github.repository }}/actions/jobs/${{ needs.e2e-nightly-test.outputs.job-id }}/logs \
              | (grep '✖' || echo "No errors found") | sed "s,\x1B\[[0-9;]*[a-zA-Z],,g"
            echo -e "\n$delimiter\n"
          } | tee -a "$GITHUB_OUTPUT"
      - name: Notify Slack
        uses: slackapi/slack-github-action@v2.0.0
        if: ${{ !cancelled() }}
        with:
          # This data can be any valid JSON from a previous step in the GitHub Action.
          # The toJSON function in "errors" should automatically quote the errors appropriately.
          webhook: ${{ secrets.SLACK_WEBHOOK }}
          webhook-type: incoming-webhook
          payload: |
            text: "Nightly Playwright Tests: ${{ needs.e2e-nightly-test.result }}", 
            blocks:
              - type: "section"
                text:
                  type: "mrkdwn"
                    text: "See run results, <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|here>" 
#      - name: Notify Slack
#      if: always()
#      env:
#        SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
#      run: |
#        STATUS="Success"
#        if [ "${{ job.status }}" != "success" ]; then
#          STATUS="@channel Failure"
#        fi
#        curl -X POST \
#           -H 'Content-type: application/json' \
#           --data "{\"text\":\"Nightly Playwright Tests: $STATUS\"}" \
#           $SLACK_WEBHOOK   